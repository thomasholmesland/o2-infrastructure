name: o2-infrastructure

services:
  portainer:
    image: portainer/portainer-ce:lts
    container_name: portainer
    restart: unless-stopped

    security_opt:
      - no-new-privileges:true

    ports:
      - "9443:9443"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data

    networks:
      - o2-network

volumes:
  portainer_data:
    name: portainer_data

networks:
  o2-network:
    external: true
